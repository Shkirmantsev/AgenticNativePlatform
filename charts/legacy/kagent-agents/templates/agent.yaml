{{- if .Values.sampleAgent.enabled }}
apiVersion: kagent.dev/v1alpha2
kind: Agent
metadata:
  name: {{ .Values.sampleAgent.name }}
  namespace: {{ .Values.namespace }}
spec:
  description: {{ .Values.sampleAgent.description | quote }}
  type: Declarative
  declarative:
    modelConfig: default-model-config
    systemMessage: |-
      You are an expert Kubernetes agent that uses tools to help users.
    tools:
      - type: McpServer
        mcpServer:
          name: kagent-tool-server
          kind: RemoteMCPServer
          toolNames:
            - k8s_get_resources
            - k8s_get_available_api_resources
    a2aConfig:
      skills:
        - id: get-resources-skill
          name: Get Resources
          description: Get resources in the Kubernetes cluster
          inputModes: [text]
          outputModes: [text]
          tags: [k8s, resources]
          examples:
            - Get all resources in the Kubernetes cluster
            - Get the pods in the default namespace
{{- end }}
