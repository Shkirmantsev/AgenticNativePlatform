# Archived Platform Composition

This directory is archived under `flux/legacy/components/`.

Do not use this monolithic path for current bootstrap or new edits.
The canonical layout is now:

- `flux/components/bundles/*`
- `flux/components/profiles/*`
- `flux/generated/clusters/*`

The old `platform-core` / `platform` composition is preserved here only as a historical reference.
