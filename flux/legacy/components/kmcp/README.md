Legacy path for the old monolithic Flux layout.
Do not modify this directory for the active generated-cluster flow; use `kmcp-core`, `kmcp-resources`, bundles, profiles, and `flux/generated/clusters/*` instead.
