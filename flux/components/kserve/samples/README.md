# KServe samples

These samples are intentionally not part of the default reconcile path.
Use them after the base platform is healthy and after you have a compatible runtime node.

- `vllm-servingruntime.yaml` defines a custom ServingRuntime for vLLM.
- `vllm-inferenceservice.yaml` shows an example InferenceService using that runtime.
