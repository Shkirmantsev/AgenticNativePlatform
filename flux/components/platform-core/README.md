Legacy path for the old monolithic Flux layout.
Do not modify this directory for the active generated-cluster flow; use the split `*-core` and `*-resources` components composed by bundles, profiles, and `flux/generated/clusters/*` instead.
