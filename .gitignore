# Local environment and IDE artifacts
.env
.env.local
.idea/
*.code-workspace

# Terraform / OpenTofu local state and generated variables
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
*.auto.tfvars
terraform.tfstate
terraform.tfstate.*
.terraform.lock.hcl
crash.log
crash.*.log

# Generated local artifacts
ansible/generated/
.kube/generated/
ansible/playbooks/.kube/
.generated/
.sops/
age.agekey
age.agekey.txt

# Keep encrypted GitOps secrets in Git, but never commit plaintext renders
flux/secrets/**/*.plaintext.yaml

# Optional local inventory override
ansible/inventory.ini
