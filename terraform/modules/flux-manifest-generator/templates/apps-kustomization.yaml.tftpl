apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ${profile_component}/applications
%{ if include_local_bootstrap_artifacts ~}
  - generated-metallb-values.yaml
%{ endif ~}
  - ../../../../overlays/${environment}
